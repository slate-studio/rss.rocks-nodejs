// Generated by CoffeeScript 1.7.1
(function() {
  var Cron, Firebase, Parser, job;

  Array.prototype.unique = function() {
    var key, output, value, _i, _ref, _results;
    output = {};
    for (key = _i = 0, _ref = this.length; 0 <= _ref ? _i < _ref : _i > _ref; key = 0 <= _ref ? ++_i : --_i) {
      output[this[key]] = this[key];
    }
    _results = [];
    for (key in output) {
      value = output[key];
      _results.push(value);
    }
    return _results;
  };

  Parser = require('../lib/parser');

  Firebase = require('firebase');

  Cron = (function() {
    function Cron() {
      this.firebaseAppName = 'rss-rocks';
      this.firebaseRef = new Firebase("https://" + this.firebaseAppName + ".firebaseio.com");
      this.usersRef = this.firebaseRef.child('users');
    }

    Cron.prototype.start = function() {
      return this.usersRef.on('value', (function(_this) {
        return function(snapshot) {
          _this.usersRef.off();
          _this.users = snapshot.val();
          _this.getFeeds();
          return _this.updateFeeds();
        };
      })(this));
    };

    Cron.prototype.getFeeds = function() {
      var feeds, profile, uid, _fn, _ref;
      feeds = [];
      _ref = this.users;
      _fn = function(uid, profile) {
        var id, s, urls;
        urls = (function() {
          var _ref1, _results;
          _ref1 = profile.subscriptions;
          _results = [];
          for (id in _ref1) {
            s = _ref1[id];
            _results.push(s.url);
          }
          return _results;
        })();
        return Array.prototype.push.apply(feeds, urls);
      };
      for (uid in _ref) {
        profile = _ref[uid];
        _fn(uid, profile);
      }
      return this.feeds = feeds.unique();
    };

    Cron.prototype.updateFeeds = function() {
      return new Parser(this.feeds, function() {
        return process.exit();
      });
    };

    return Cron;

  })();

  job = new Cron();

  job.start();

}).call(this);
